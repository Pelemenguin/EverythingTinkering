# 通用
每当整合包添加新内容时，检查这里。
- 若为石头、木头等，向匠魂材料添加相应变种。
- 若为矿物，它是否可以：
    - 被 `uncertain` 复制？

# 计划

## 计划修改材料
- 添加 `calcite`，`tuff`，`prismarine`，`dripstone` 作为 `rock` 变种。
## 计划创造材料
- 添加 `soul` 作为 `glass` 的变种。
## 计划创建配方
- 添加材料 `glass#*_stained` 配方。
## 杂项
- 触发 `relaying` 时播放粒子特效。
- 为属性添加UI显示。
- 修复 `on_living_hurt` 出现在读取工具损坏时出现 `null` 的情况。

# 弃用

## 计划创建配方
- 尝试添加配方：`clay` 部件烧制为 `brick` 部件。
